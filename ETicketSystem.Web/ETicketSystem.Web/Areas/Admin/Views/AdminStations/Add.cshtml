@model StationFormModel

@{
    ViewData["Title"] = "Add Station";
}

<h2 class="text-center">@ViewData["Title"]</h2>

<div class="container-fluid">
    @Html.Partial("_AdminStationForm", Model)
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}

